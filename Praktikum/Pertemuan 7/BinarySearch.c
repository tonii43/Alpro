#include <stdio.h>

int binarySearch(N, T, X) {
    int atas = 1;
    int bawah = N;
    int tengah = (atas + bawah) / 2;

    while ((atas < bawah) && (X != T[tengah])) {
        
    }
}
int main() {
    
}